<?php
/**
 * Created by PhpStorm.
 * User: Boris
 * Date: 25.08.2018
 * Time: 22:11
 */

namespace base;


class Router
{
    /*private $routes = [];
    private $rexRoutes = [];

    private  $current_route = null;

    public function param($name)
    {
        if (isset($this->current_route[$name]))
            $this->current_route[$name];
    }

    public function add($path, $command)
    {
        $this->routes[$path] = $command;
        if (preg_match_all('<(.*)>', $path, $matches)) {
            $params = $matches[1];
            $rex_route = preg_replace('<(.*)>', '(.*)', $path);
            $this->rexRoutes['#' . $rex_route . '#'] = $params;
        }
        $this->routes[$path] = $command;
    }*/
}